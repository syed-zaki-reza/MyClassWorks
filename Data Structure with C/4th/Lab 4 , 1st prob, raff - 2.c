#include <stdio.h>
#include <conio.h>
#define INFINITY 9999

void insert(int n, ar[][]);

void main()
{
    int n = 2, b[2][2];

    printf("\n Enter the number of nodes in the graph : ");
    scanf("%d", &n);
    printf("\n Enter the adjacency matrix : ");
    read(adj, n);

    printf("\n The adjacency matrix is : ");
    display(adj, n);

    for(i=0;i<n;i++)
    {
        pr
    }

}
